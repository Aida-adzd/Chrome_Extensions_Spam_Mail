# Path to the CSV file that contains the training data for the email classifier model
path_to_training_data_csv = r"your path"

# Path to the location where the trained email classifier model is (or will be) saved
path_to_saved_model = r"your path"

# Path to the HTML file that is used to create the popup interface for the email classifier
path_to_popup_html = r"your path"

# Replace "your path" with the actual paths to your files.
# Ensure that the paths are correct and the files exist at the specified locations.